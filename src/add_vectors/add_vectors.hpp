
void VectorFill(float * v, int size);

void VectorAdd(float const * fst, float const * snd, float * res, int size, bool use_gpu=false);

void VectorPrint(char const * name, float const * v, int size);
